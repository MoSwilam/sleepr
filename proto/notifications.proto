syntax = "proto3";

package notifications;

service NotificationService {
  rpc notifyEmail (SendNotificationRequest) returns (Empty) {};
}

message SendNotificationRequest {
  string email = 1;
  string text = 2;
}

message Empty {}